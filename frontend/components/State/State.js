"use client";

const State = () => {
  const H2Styled = {
    textAlign: "center",
    marginBottom: "2em",
    color: "blue",
    backgroundColor: "#fff",
    padding: "1.5em",
    borderRadius: "10px",
  };

  return <h2 style={H2Styled}>Registering Voters</h2>;
};

export default State;
